export interface PathProps {
  sendOtp: string;
  validateData: string;
  validateOtp: string;
  register: string;
  login: string;
  logout: string;
  forgotPassword: string;
  resetPassword: string;
}
